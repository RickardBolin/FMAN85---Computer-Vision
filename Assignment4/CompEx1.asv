clear
close all
restoredefaultpath
addpath('assignment4data')
load('compEx1data.mat')

house1 = imread('house1.jpg');
house2 = imread('house2.jpg');

X = pflat(X);

plane = [1 1 1 1];


rms = @(plane, points) sqrt(mean((plane*points).^2)./(sum(plane(1:3).^2)));

plane = fminsearch(rms, plane)